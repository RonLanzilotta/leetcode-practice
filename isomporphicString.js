var isIsomorphic = function(s, t) {
    let sCharacters = s.split();
    let tCharacters = t.split();
    let container = sCharacters

    for (let i = 0; i < sCharacters.length; i++) {
        container[i] = tCharacters[i]
    }


    if
};